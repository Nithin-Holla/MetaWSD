meta_learner: proto_net
meta_model: seq
learner_model: bert
learner_params:
  hidden_size: 192
  num_outputs:
    wsd: var
  embed_dim: 768
  dropout_ratio: 0
vectors: bert
meta_lr: 0.000001
num_shots:
  wsd: 32
num_updates: 5
num_test_samples:
  wsd: 32
num_train_episodes:
  wsd: 10000
num_val_episodes:
  wsd: 84
num_test_episodes:
  wsd: 129
num_meta_epochs: 25
early_stopping: 2
device: cuda:0
